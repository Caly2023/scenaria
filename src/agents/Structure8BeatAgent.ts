import { StructureAgent } from './StructureAgent';
import { WorkflowStage } from '../types';

export class Structure8BeatAgent extends StructureAgent {
  readonly stageId: any = '8-Beat Structure';
}
